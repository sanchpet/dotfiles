# Work identity for repos under ~/IWE/magnit/ (see ~/.gitconfig includeIf).
# Signing is off here — corporate repos don't use the personal SSH key.
[user]
	name = petrov_ad
	email = petrov_ad@magnit.ru
[commit]
	gpgsign = false
[tag]
	gpgsign = false
